# 工作流约束

## 功能概述

在 AGENTS.md 中新增「工作流约束」章节，规范代码修改和提交流程。

## 变更内容

1. **禁止擅自提交**: 未经用户明确指令，不得执行 `git commit` 或 `git push`
2. **修改后必须 Code Review + 启动验证**: 每次代码修改后启动 subagent review，通过后运行应用确认正常

## 文件变更清单

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `AGENTS.md` | 修改 | 新增「工作流约束」章节 |
