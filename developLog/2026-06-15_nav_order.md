# 导航栏顺序调整

## 功能概述

将「其他功能」模块移至导航栏 SCROLL 区末尾，并在 AGENTS.md 中添加导航顺序约束。

## 实现步骤

1. 调整 `main_window.py` 中 `__initNavigation` 的模块顺序
2. 在 AGENTS.md 的界面设计约束中新增导航栏顺序规则

## 导航栏顺序

开始 → 生涯 → 搜索 → 对局信息 → 游戏流 → 其他功能

## 文件变更清单

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `app/view/main_window.py` | 修改 | 调整导航栏模块顺序 |
| `AGENTS.md` | 修改 | 新增导航栏顺序约束 |
